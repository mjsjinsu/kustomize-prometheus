namespace: zcp-system

resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
  - rules.yaml
  - additional-scrape-configs.yaml

patchesJson6902:
- target:
    group: monitoring.coreos.com
    version: v1
    kind: Prometheus
    name: prometheus
  path: patch.yaml
